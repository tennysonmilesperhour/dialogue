# Support page copy

This is the source copy for the live `/support` route. Keep it aligned with
the features in the submitted binary.

## dialogue support

**What is dialogue?**
A ledger for your attention. When you open a watched app, dialogue asks why.
When you leave, it asks whether that held up. The record is the product.

**Does dialogue block apps?**
No. Never. The Enter path is always available. What changes over time is how
light or deliberate the gate is, and that follows your own match rate.

**Why can't dialogue see my app names?**
Apple's Screen Time system gives apps opaque tokens instead of names, by
design. The names in your ledger are the labels you wrote during setup.

**Why is my session length approximate?**
iOS does not tell apps exactly when another app closes. dialogue triangulates
from the signals it does get and labels estimates honestly instead of faking
precision.

**What does the subscription add?**
The current build has no subscription. Optional Sync is planned for a later
version, after the local ledger has been tested in public.

**Contact**
Open a public support request at
https://github.com/tennysonmilesperhour/dialogue/issues/new. Do not include
private ledger entries or other personal information. Add a private support
channel before public launch.
